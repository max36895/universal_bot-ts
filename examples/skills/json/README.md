# Пример: данные локального хранилища

Содержит файл `UsersData.json` — пример структуры данных, которые `FileAdapter` сохраняет для пользователей. Используется другими примерами как стартовое состояние хранилища.

Сам по себе пример не запускается — это файл данных.
