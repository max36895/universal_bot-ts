/**
 * Универсальный фреймворк для создания голосовых приложений и чат-ботов
 * @packageDocumentation
 * @module universal-bot
 * @version 1.2.0
 * @author Maxim-M <maximco36895@yandex.ru>
 *
 * @example
 * ```typescript
 * import { mmApp, BotController, run } from 'universal-bot';
 *
 * class MyController extends BotController {
 *   action(intentName: string | null): void {
 *     if (intentName === 'welcome') {
 *       this.text = 'Привет! Чем могу помочь?';
 *       this.buttons
 *         .addButton('Помощь')
 *         .addButton('Выход');
 *     }
 *   }
 * }
 *
 * run({
 *   appConfig: { ... },
 *   appParam: { ... },
 *   controller: new MyController()
 * });
 * ```
 */

// ===== Ядро приложения =====
/**
 * Основные компоненты ядра фреймворка
 * @remarks
 * Включает базовые классы и интерфейсы для работы с ботом
 */
export * from './core';

/**
 * Глобальная конфигурация и параметры приложения
 * @remarks
 * Содержит настройки, константы и типы для всего приложения
 */
export * from './mmApp';

// ===== Взаимодействие с web api =====
/**
 * Модули для работы с внешними API
 * @remarks
 * Предоставляет интерфейсы и классы для взаимодействия с различными веб-сервисами
 */
export * from './api';

// ===== Второстепенные компоненты =====
/**
 * Вспомогательные компоненты
 * @remarks
 * Включает компоненты для работы с кнопками, карточками, изображениями,
 * звуком и другими UI элементами
 */
export * from './components';

// ===== Поддерживаемы платформы =====
/**
 * Интеграции с различными платформами
 * @remarks
 * Содержит реализации для работы с Алисой, Марусей, Telegram и другими платформами
 */
export * from './platforms';

// ===== Базовый контроллер для написания логики приложения =====
/**
 * Контроллеры приложения
 * @remarks
 * Предоставляет базовый класс BotController для реализации бизнес-логики бота
 */
export * from './controller';

// ===== Модели для взаимодействия с бд =====
/**
 * Модели данных
 * @remarks
 * Содержит классы и интерфейсы для работы с базой данных и хранением состояния
 */
export * from './models';

// ===== Дополнительные утилиты =====
/**
 * Вспомогательные утилиты
 * @remarks
 * Включает инструменты для работы с текстом, форматирования данных и другие
 * полезные функции
 */
export * from './utils';
