export const T_VIBER = 'viber';

/**
 * Минимальная версия API Viber по умолчанию.
 * Версия 7 необходима для поддержки carousel content (rich_media).
 * Если пользователь явно не указал версию, используется эта.
 */
export const VIBER_DEFAULT_API_VERSION = 7;
