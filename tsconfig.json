{
  "compilerOptions": {
    "module": "commonjs",
    "target": "es6",
    "lib": [
      "es2016",
      "dom"
    ], // Какие библиотеки включать (тут есть async/await, например)
    "moduleResolution": "node", // Формат модулей (на входе)
    "removeComments": true,
    "sourceMap": true, // Нужно ли юзать удобные для дебага source map'ы (да, нужно)
    "outDir": "dist", // Куда выплевывать скомпиленный код
    "baseUrl": ".", // Где сам проект находится
    "paths": {
      /*"@api*//*": ["src/MM/bot/api*//*"],
      "@components*//*": ["src/MM/bot/components*//*"],
      "@controller*//*": ["src/MM/bot/controller*//*"],
      "@core*//*": ["src/MM/bot/core*//*"],
      "@models*//*": ["src/MM/bot/models*//*"],
      "@utils*//*": ["src/MM/bot/utils*//*"],*/
      "*": ["node_modules/*", "src/types/*"] // Где брать дополнительные типы
    }
  },
  "include": [
    "src/**/*",
    "demo/**/*",
    "tests/**/*"
  ],
  "exclude": [
    "node_modules"
  ],
  "types": [ "mocha", "chai" ],
  "typeRoots": [
    "./node_modules/@types"
  ]
}
